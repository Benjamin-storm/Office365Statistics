<Page
    x:Class="Office365Statistics.View.ReportView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Office365Statistics.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    DataContext="{Binding Source={StaticResource ViewModelLocator}, Path=Report}">

    <Grid>
        <StackPanel
            HorizontalAlignment="Center"
            VerticalAlignment="Center">
            <TextBlock 
                FontSize="54" 
                Foreground="Black"
                Text="{Binding Path=NumberOfFiles}" />
            <Button 
                Content="Reload" 
                FontSize="54" 
                Foreground="Black"
                Command="{Binding Path=GetNumberOfFiles}"/>
        </StackPanel>

    </Grid>
</Page>
